import React from 'react';
import PropTypes from 'prop-types';
import './DevNote.css';

const DevNote = () => (
  <div className="DevNote">
    DevNote Component
  </div>
);

DevNote.propTypes = {};

DevNote.defaultProps = {};

export default DevNote;
